<Page x:Class="IssueTracker.WPF.Views.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewModels="clr-namespace:IssueTracker.WPF.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Login">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <Viewbox>
            <TextBlock>Login</TextBlock>
        </Viewbox>
        <StackPanel Margin="0" Grid.Row="1">
            <TextBox Height="30" Name="UsernameText"/>
            <PasswordBox Height="30" Name="PasswordText" />
            <Button Height="30" Content="Login" Name="LoginButton" Click="LoginClick"/>
        </StackPanel>

    </Grid>
</Page>
